services:
  database:
    container_name: ${DATABASE_SERVICE}
    image: postgis/postgis
    ports:
      - 5432:5432
    environment:
      - POSTGRES_DB=${DATABASE_NAME}
      - POSTGRES_USER=${DATABASE_USER}
      - POSTGRES_PASSWORD=${DATABASE_PASSWORD}

  application:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 3000:3000
    environment:
      DATABASE_URL: postgresql://${DATABASE_USER}:${DATABASE_PASSWORD}@database:5432/${DATABASE_NAME}
    depends_on:
      - database

  traefik:
    image: traefik:${TRAEFIK_IMAGE_VERSION:-latest}
    restart: unless-stopped
    ports:
      - 80:80
      - 443:443
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./acme.json:/etc/traefik/acme.json
      - ./traefik.yaml:/etc/traefik/traefik.yaml
      - ./traefik-dynamic.yaml:/etc/traefik/traefik-dynamic.yaml
    labels:
      # Traefik
      - traefik.enable=true
      - traefik.docker.network=reverse-proxy
      # Middlewares
      - traefik.http.middlewares.redirect-to-https.redirectscheme.scheme=https
      - traefik.http.middlewares.redirect-to-https.redirectscheme.permanent=true
      # External HTTP
      - traefik.http.routers.traefik-external-http.rule=Host(`${TRAEFIK_EXTERNAL_FQDN}`)
      - traefik.http.routers.traefik-external-http.entrypoints=web
      - traefik.http.routers.traefik-external-http.service=api@internal
      - traefik.http.routers.traefik-external-http.middlewares=redirect-to-https
      # External HTTPS
      - traefik.http.routers.traefik-external-https.rule=Host(`${TRAEFIK_EXTERNAL_FQDN}`)
      - traefik.http.routers.traefik-external-https.entrypoints=websecure
      - traefik.http.routers.traefik-external-https.service=api@internal
      - traefik.http.routers.traefik-external-https.tls=true
      - traefik.http.routers.traefik-external-https.tls.certresolver=letsencrypt
