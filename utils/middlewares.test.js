const { assert } = require("chai");
const { isAuthenticated } = require("./middlewares");

describe("utils.middlewares", function () {
  describe("#isAuthenticated()", function () {
    it("should restric the access to paths", function () {
      // TODO
    });
  });
});
